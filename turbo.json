{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "build/**",
        ".next/**"
      ]
    },
    "rpch-exit-node#build": {
      "env": [
        "HOPRD_API_ENDPOINT",
        "HOPRD_API_TOKEN",
        "RESPONSE_TIMEOUT"
      ],
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "build/**",
        ".next/**"
      ]
    },
    "format": {
      "outputs": []
    },
    "lint": {
      "dependsOn": [
        "format"
      ],
      "outputs": []
    },
    "test": {
      "outputs": [
        "coverage/**",
        "report.json"
      ]
    }
  }
}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "discovery-platform.fullname" . }}-test-connection"
  labels:
    {{- include "discovery-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "discovery-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
